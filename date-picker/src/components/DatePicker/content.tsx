import React from 'react'
import cls from 'classnames'

export function DatePickerPanelContent() {
  const panelContentPrefixCls = 'date-picker'

  return <div className={cls(panelContentPrefixCls)}>content</div>
}